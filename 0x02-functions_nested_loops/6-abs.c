#include <main.h>
/**
 * main - a function that computes absolute value..
 * Return: Absolute value
 */

int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (x * -1);
	}
	_putchar('\n')
}

