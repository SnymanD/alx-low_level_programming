#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * Description: main - Print all single digit numbers.
 * Return: Always 0 (Success)
 */

int main(void)
{
	int s;

	for (s = 0; s < 10; s++)
	printf("%d", s);
	printf("\n");

	return (0);
}
