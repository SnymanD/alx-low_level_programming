#include <stdio.h>

/**
 * Description: main - Print the alphabet
 * Return: Always 0 (Success)
 */

int main(void)
{
	char s;

	for (s = 'a'; s <= 'z'; s++)
	putchar(s);
	putchar('\n');
	return (0);
}
